beantrage Drucke von Drucker;
beantrage Zeit von Uhr;
beantrage Zergliedere_JSON von Textverarbeitung;
beantrage Http von Netzwerk;

funk Bewerbung() ergibt Zeichenkette {
  "Das ist meine wunderbare Bewerbung."
}

funk Einschreibung(Zahl Matrikelnummer) ergibt Nichts {
  setze Zahl _ auf Matrikelnummer;
}

funk Studium() ergibt Nichts {
    Drucke(Nichts);
    überweise;

    setze Zeichenkette Körper auf "";

    Drucke("Fetching...");
    setze Zahl Antwort auf Http(
        "GET" /
        "https://swp.webspeiseplan.de/index.php?token=55ed21609e26bbf68ba2b19390bf7961&model=menu&location=9601&languagetype=2&_=1697743364185" /
        "" /
        [
            erstelle {
                Zeichenkette Schlüssel auf "Referer" /
                Zeichenkette Wert auf "https://swp.webspeiseplan.de/menu" /
            }
        ] /
        &Körper /
    );
    Drucke("Fetched.");

    // Drucke("Antwort:" / Antwort);

    Drucke("Körper: " / Körper);

    setze Liste von Speicherbox KörperParsed auf Zergliedere_JSON(Körper) als Liste von Speicherbox;
   Drucke(KörperParsed.Länge());

    // setze Zeichenkette Input auf "[ \"foo\" , \"bar\" ]";
    //setze Zeichenkette Input auf "{ \"foo\": [ \"foo\", \"bar\" ] }";
    //setze Liste von Zeichenkette Test auf Zergliedere_JSON(Input) als Liste von Zeichenkette;
}

