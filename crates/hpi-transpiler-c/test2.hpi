beantrage Drucke von Drucker;
beantrage Formatiere von Textverarbeitung;
beantrage Zeit von Uhr;
beantrage Reinigung von FM;
beantrage ReinigungsPlan von FM;

funk Bewerbung() ergibt Zeichenkette { "Bitte bitte" }

funk Einschreibung(Zahl Matrikelnummer) ergibt Nichts {}

funk A() ergibt Zeichenkette {
    setze Zeichenkette foo auf "hallo";
    foo
}

funk B() ergibt Liste von Zahl {
    setze Liste von Zahl foo auf [ 1 ];
    foo
}

funk Studium() ergibt Nichts {
    // setze Zeichenkette b auf A();
    // Reinigung();
    // solange ja {
        // Schlummere(1F);
    // }


    //setze Zahl z auf 0;
    //solange z < 1000 {
    //setze Liste von Zahl foo auf [ 1 ];
        //Drucke("Hallo" / foo);
        //z += 1;
    //}

    //setze Liste von Zahl _ auf B();
    //B();
    //{
        //setze Liste von Zahl _ auf [ 1 ];
    //}

    //Reinigung();

    //setze Liste von Zeichenkette a auf [ "aa" ];
    {
        //setze Liste von Zeichenkette a auf [ "aa" ];
        setze Liste von Zahl a auf [ 42 / 69 ];
        //setze Zeichenkette a auf "hello world";
    }
    Reinigung();
    //Drucke(a);


    //Drucke("*******************************************");

    //ReinigungsPlan();

    //solange ja {
        //Schlummere(1F);
    //}
}
